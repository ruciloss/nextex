import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Test | NexteX",
    description:
        "A simple template for building modern websites with Next.js & StyleX.",
};

const Homepage = () => {
    return <span>dddddddd</span>;
};

export default Homepage;
