import * as stylex from "@stylexjs/stylex";

export const baseStyles = stylex.create({
    input: {
        appearance: "none",
        background: "none",
        borderWidth: 0,
        color: "inherit",
        font: "inherit",
        outline: "none",
    },
});
